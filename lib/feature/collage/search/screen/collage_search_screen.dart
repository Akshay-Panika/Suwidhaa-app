import 'package:flutter/material.dart';

class CollageSearchScreen extends StatelessWidget {
  const CollageSearchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
